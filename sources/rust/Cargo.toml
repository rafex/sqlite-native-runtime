[workspace]
members  = ["ether-sqlite-core", "ether-sqlite-ffm", "ether-sqlite-jni"]
resolver = "2"

[profile.release]
panic         = "abort"
opt-level     = 3
lto           = true
codegen-units = 1
strip         = true

[profile.dev]
panic = "abort"
